# Render Blueprint: optional one-click deploy from repo
# https://render.com/docs/blueprint-spec
services:
  - type: web
    name: olympic_tracker
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: "20"
